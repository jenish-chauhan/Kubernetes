first create an ec2 instance and install docker on it

and then create kind cluster on it

and then install argo cd on it

and forward port for the argocd run on the ec2 instance

then login into the argo through cli and add cluster to the argo cd and also add repository to the argo cd through the argocd UI
