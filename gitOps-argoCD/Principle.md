Key principles of GitOps (must remember):

Declarative:
--YAML defines what you want, not how

Versioned & immutable:
--Every change is tracked in Git

Pull-based deployment:
--Cluster pulls changes
--Not CI pushing blindly

Automated reconciliation:
--Continuous sync
